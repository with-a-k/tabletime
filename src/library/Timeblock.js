let moment = require('moment-timezone');

class Timeblock {
  constructor(day, start, length) {
    this.day = day;
    this.start = start;
    this.length = length;
  }

  prettyPrint() {
    
  }
}
